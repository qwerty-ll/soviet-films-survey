/**
 * ============================================================
 * Google Apps Script для приёма данных из анкеты
 * ============================================================
 * 
 * ИНСТРУКЦИЯ ПО НАСТРОЙКЕ:
 * 
 * 1. Создайте новую Google Таблицу (Google Sheets):
 *    https://sheets.google.com → "Создать таблицу"
 * 
 * 2. Откройте редактор скриптов:
 *    Расширения → Apps Script
 * 
 * 3. Удалите весь код по умолчанию и вставьте код ниже
 *    (начиная со строки "// === HEADERS ===")
 * 
 * 4. Нажмите "Сохранить" (Ctrl+S)
 * 
 * 5. Опубликуйте как веб-приложение:
 *    - Нажмите "Развертывание" → "Новое развертывание"
 *    - Тип: "Веб-приложение"
 *    - Описание: "Анкета"
 *    - Выполнять как: "Я" (ваш аккаунт)
 *    - Доступ: "Все" (Anyone)
 *    - Нажмите "Развернуть"
 * 
 * 6. Скопируйте URL веб-приложения и вставьте его в файл
 *    script.js вместо 'YOUR_GOOGLE_APPS_SCRIPT_URL_HERE'
 * 
 * 7. Готово! Данные из анкеты будут записываться в таблицу.
 * 
 * ВАЖНО: При обновлении кода нужно создавать НОВОЕ развёртывание
 * (Развертывание → Управление развертываниями → Изменить)
 * ============================================================
 */

// === HEADERS — порядок столбцов в таблице ===
var HEADERS = [
  '№ ответа',
  'Дата и время',
  '1. Ваш возраст',
  '2. Смотрите ли советские фильмы?',
  '3. Отношение к ремейкам',
  '4. Ценности советского кино актуальны?',
  '5. Цель создания ремейков',
  '5. Другое (уточнение)',
  '6. «А зори здесь тихие» — советская версия',
  '6. «А зори здесь тихие» — новая версия',
  '6. «А зори здесь тихие» — что понравилось больше',
  '6. «А зори здесь тихие» — оценка (1-5)',
  '6. «А зори здесь тихие» — комментарий',
  '7. «Мастер и Маргарита» — советская версия',
  '7. «Мастер и Маргарита» — новая версия',
  '7. «Мастер и Маргарита» — что понравилось больше',
  '7. «Мастер и Маргарита» — оценка (1-5)',
  '7. «Мастер и Маргарита» — комментарий',
  '8. «Москва слезам не верит» — советская версия',
  '8. «Москва слезам не верит» — новая версия',
  '8. «Москва слезам не верит» — что понравилось больше',
  '8. «Москва слезам не верит» — оценка (1-5)',
  '8. «Москва слезам не верит» — комментарий',
  '9. «Ну, погоди!» — советская версия',
  '9. «Ну, погоди!» — новая версия',
  '9. «Ну, погоди!» — что понравилось больше',
  '9. «Ну, погоди!» — оценка (1-5)',
  '9. «Ну, погоди!» — комментарий',
  '10. «Простоквашино» — советская версия',
  '10. «Простоквашино» — новая версия',
  '10. «Простоквашино» — что понравилось больше',
  '10. «Простоквашино» — оценка (1-5)',
  '10. «Простоквашино» — комментарий',
  '11. «Чебурашка» — советская версия',
  '11. «Чебурашка» — новая версия',
  '11. «Чебурашка» — что понравилось больше',
  '11. «Чебурашка» — оценка (1-5)',
  '11. «Чебурашка» — комментарий',
  '12. «Иван Васильевич» — советская версия',
  '12. «Иван Васильевич» — новая версия',
  '12. «Иван Васильевич» — что понравилось больше',
  '12. «Иван Васильевич» — оценка (1-5)',
  '12. «Иван Васильевич» — комментарий',
  '13. Отличия старых и новых версий',
  '13. Другое (уточнение)',
  '14. Ценности в новых экранизациях'
];

function doPost(e) {
  try {
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
    var data = JSON.parse(e.postData.contents);
    
    // Если таблица пустая — создаём заголовки и форматируем
    if (sheet.getLastRow() === 0) {
      sheet.appendRow(HEADERS);
      
      // Форматирование заголовков
      var headerRange = sheet.getRange(1, 1, 1, HEADERS.length);
      headerRange.setFontWeight('bold');
      headerRange.setBackground('#1a1a2e');
      headerRange.setFontColor('#d4a843');
      headerRange.setWrap(true);
      headerRange.setVerticalAlignment('middle');
      
      // Фиксируем первую строку
      sheet.setFrozenRows(1);
      
      // Устанавливаем ширину столбцов
      sheet.setColumnWidth(1, 80);   // № ответа
      sheet.setColumnWidth(2, 140);  // Дата и время
      for (var i = 3; i <= HEADERS.length; i++) {
        sheet.setColumnWidth(i, 180);
      }
    }
    
    // Номер ответа = последняя строка (не считая заголовок)
    var responseNumber = sheet.getLastRow();
    data['№ ответа'] = responseNumber;
    
    // Формируем строку данных строго по порядку заголовков
    var row = [];
    for (var i = 0; i < HEADERS.length; i++) {
      row.push(data[HEADERS[i]] !== undefined ? data[HEADERS[i]] : '');
    }
    
    sheet.appendRow(row);
    
    // Подкрашиваем чётные/нечётные строки для читаемости
    var lastRow = sheet.getLastRow();
    var rowRange = sheet.getRange(lastRow, 1, 1, HEADERS.length);
    if (lastRow % 2 === 0) {
      rowRange.setBackground('#f8f8f8');
    } else {
      rowRange.setBackground('#ffffff');
    }

    // АВТО-ОБНОВЛЕНИЕ ГРАФИКОВ (опционально, может замедлять ответ на 1-2 сек)
    try {
      if (typeof generateFullDashboard === 'function') {
        generateFullDashboard(true); // true = запуск без всплывающих окон
      }
    } catch (e) {
      console.log('Ошибка авто-обновления графиков: ' + e.message);
    }
    
    return ContentService
      .createTextOutput(JSON.stringify({ status: 'success', row: responseNumber }))
      .setMimeType(ContentService.MimeType.JSON);
      
  } catch (error) {
    return ContentService
      .createTextOutput(JSON.stringify({ status: 'error', message: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

// Тест: открыть URL в браузере покажет что скрипт работает
function doGet(e) {
  return ContentService
    .createTextOutput('Скрипт анкеты работает! Используйте POST для отправки данных.')
    .setMimeType(ContentService.MimeType.TEXT);
}

// Тест: открыть URL в браузере покажет что скрипт работает
function doGet(e) {
  return ContentService
    .createTextOutput('Скрипт анкеты работает! Используйте POST для отправки данных.')
    .setMimeType(ContentService.MimeType.TEXT);
}
